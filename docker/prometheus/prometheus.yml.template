global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: magi
    static_configs:
      - targets: ['magi-server:${MAGI_HTTP_PORT}']
        labels:
          service: magi
